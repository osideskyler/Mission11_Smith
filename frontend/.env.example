# Leave empty for local dev (Vite proxies /api to the backend — see vite.config.ts).
# For production against a separate API host, set the base URL with no trailing slash, e.g.:
# VITE_API_URL=https://your-api.azurewebsites.net
VITE_API_URL=
